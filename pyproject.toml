[project]
name = "ironview"
version = "0.1.0"
description = "Desktop reverse-engineering workbench built with libbfd, radare2, and GNU binutils."
requires-python = ">=3.13"
dependencies = [
    "pyside6==6.10.2",
    "r2pipe>=1.9.8",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
